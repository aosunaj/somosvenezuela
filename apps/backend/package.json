{
  "name": "backend",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "echo \"[lint] backend: sin linter configurado en esta fase\" && exit 0"
  },
  "dependencies": {
    "@fastify/rate-limit": "^11.1.0",
    "ai": "workspace:*",
    "argon2": "^0.44.0",
    "core": "workspace:*",
    "db": "workspace:*",
    "fastify": "^5.9.0",
    "fastify-type-provider-zod": "^7.0.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
